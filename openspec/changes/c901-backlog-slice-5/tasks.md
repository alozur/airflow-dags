# Tasks: C901 Backlog Slice 5 (issue #272)

Base: worktree `/home/alozur/src/github.com/alozur/airflow-dags-wt-272-s5` @ `origin/main 7e3e689`.
Branches `refactor/272-c901-slice-5-pr1` .. `-pr9`, stacked to `dev`, then one release PR `dev -> main`.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | PR1 ~125 · PR2 ~185 · PR3 ~200 · PR4 ~320 ⚠ (contingency 4a/4b) · PR5 ~240 · PR6 ~270 · PR7 ~190 · PR8 ~185 · PR9 ~275 (total ~1990) |
| 400-line budget risk | Medium — every PR is under 400; PR4 is the tight one (~80 lines headroom) |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3 → PR4 (→ 4a/4b contingency) → PR5 → PR6 → PR7 → PR8 → PR9 (→ 9a/9b contingency) → release PR |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Medium

### Suggested Work Units

| Unit | Goal | PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | Lift `_fetch_enrichable_video_details` (481-505) out of `get_video_details`; duration-leak characterization tests | PR1 | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_youtube_channel.py` | N/A — pure API-response helper, no scheduling surface | `git revert` PR1; only together with/after PR4 (token drop) |
| 2 | Lift `_evaluate_finished_stream_candidate` (374-418) out of `filter_finished_streams` | PR2 | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_youtube_channel.py` | N/A — pure per-candidate helper | `git revert` PR2; only together with/after PR4 |
| 3 | Lift `_parse_agenda_dates` + `_locate_target_date_offset` out of `extract_session_date` | PR3 | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_youtube_channel_extended.py` | N/A — pure parsing helpers | `git revert` PR3; only together with/after PR4 |
| 4 | `extract_agenda_section` characterization tests (own commit) + `_find_agenda_for_video`/`_locate_target_section` lift + `youtube_channel.py` C901 prune + counter 7→6 | PR4 (4a/4b contingency) | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_youtube_channel_extended.py` | N/A — pure text-scan helpers | `git revert` PR4 (or 4a+4b together); pairs with PR1-3 |
| 5 | Lift `_chapter_start_secs`/`_chapter_end_secs`/`_mark_overlapping_chapters` out of `_dedup_overlapping_chapters` | PR5 | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_download.py` | N/A — pure chapter-list helpers | `git revert` PR5; only together with/after PR7 |
| 6 | Lift `_find_srt_chunks_for_video`/`_collect_chunk_chapters` out of `identify_interesting_chapters` | PR6 | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_download.py` | N/A — pure SRT-index helpers | `git revert` PR6; only together with/after PR7 |
| 7 | Lift `_identify_chapters_for_chunk` (closure-preserving) out of `_analyze_single_chunk`; `download.py` C901 prune + counter 6→5 | PR7 | `uv run pytest -o addopts= tests/congress_videos/modules/youtube/test_download.py` | N/A — pure LLM-call helper, no I/O boundary change | `git revert` PR7 (or must accompany PR5/PR6 revert) |
| 8 | Lift `_download_subtitle_files` out of `download_youtube_subtitles` | PR8 | `uv run pytest -o addopts= tests/utils/test_youtube_downloader.py` | N/A — pure subtitle-download helper | `git revert` PR8; only together with/after PR9 |
| 9 | Lift `_log_available_streams`/`_select_video_stream` out of `download_with_pytubefix`; `_check_live_status_guard`/`_try_pytubefix_download` out of `download_youtube_video_for_upload`; `utils/youtube_downloader.py` C901 prune + counter 5→4 (9a/9b contingency) | PR9 | `uv run pytest -o addopts= tests/utils/test_youtube_downloader.py` | N/A — pure stream-selection/guard helpers; ffmpeg subprocess boundary untouched | `git revert` PR9 (or 9a+9b); must accompany PR8 revert |

Per-PR closing tasks (identical for every PR, added as the last numbered task in each phase):
run the AST-equality proof script and capture `OK` lines; `uv run ruff check .`; `uv run ruff format --check .`; full `uv run pytest` (must be >= base **5274 passed, 34 skipped** — new tests only add); the isolated hidden-regression measurement `uvx ruff check --select C901 --no-cache --config 'lint.per-file-ignores = {}' --output-format concise <file>` for the file(s) that PR touched.

## PR1 — `get_video_details` (`congress_videos/modules/youtube/youtube_channel.py:481-505`)

- [ ] 1.1 Baseline: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' congress_videos/modules/youtube/youtube_channel.py`; confirm `get_video_details` measures 11.
- [ ] 1.2 RED-first tests in `tests/congress_videos/modules/youtube/test_youtube_channel.py`, class `TestFetchEnrichableVideoDetails`: empty `items`→`None`; missing `liveStreamingDetails`→`None` (not `KeyError`); `actualEndTime` 11h59m ago with `min_hours_since_end=12`→`None`, 12h01m→2-tuple `(video_details, live_details)`; `"…Z"` suffix parsed via `+00:00`; a raising `.execute()` propagates (`pytest.raises`). Confirm RED (helper does not exist).
- [ ] 1.3 RED-first characterization tests in the same file, class `TestGetVideoDetailsDurationLeak` (same commit as 1.2, against untouched pre-lift source, green as characterization not RED): `"PT1H2M3S"` then `"P0D"` in one call → second entry `duration_seconds == 0` and `duration_formatted == "1:02:03"` (stale cross-iteration leak); a lone `"P0D"` → `RuntimeError` mentioning `hours`. These pin the narrowed 481-505 lift boundary — do NOT widen to the whole for-body (see design "Deviations from the exploration").
- [ ] 1.4 Lift `_fetch_enrichable_video_details(youtube, video_id, min_hours_since_end) -> tuple[dict, dict] | None` from base 481-505, immediately above `get_video_details`. Replace with `details = _fetch_enrichable_video_details(youtube, video_id, min_hours_since_end)` / `if details is None: continue` / `video_details, live_details = details`, applying normalization (a) x3 (`continue`→`return None`) and (c) (appended `return`). Duration parsing (513-527) and dict build stay in the caller, byte-identical.
- [ ] 1.5 Complexity re-measure: confirm `get_video_details` ≤10 (predicted 9) and `_fetch_enrichable_video_details` ≤10 (predicted 4).
- [ ] 1.6 PR1 closing tasks (see "Per-PR closing tasks" above), scoped to `congress_videos/modules/youtube/youtube_channel.py`; also `git diff --shortstat` ≤400 and `git diff -- tests/ | rg '^-[^-]'` empty.
- [ ] 1.7 Commit: `refactor(youtube-channel): lift enrichable-video-details fetch out of get_video_details (#272)`. PR1 body: dependency diagram 📍 PR1, base `dev`, `Refs #272`, note PR1 may only be reverted together with/after PR4.

## PR2 — `filter_finished_streams` (`congress_videos/modules/youtube/youtube_channel.py:374-418`)

- [ ] 2.1 Rebase onto PR1's tip. Baseline: confirm `filter_finished_streams` measures 13.
- [ ] 2.2 RED-first tests in `tests/congress_videos/modules/youtube/test_youtube_channel.py`, class `TestEvaluateFinishedStreamCandidate`: falsy `video_id`→`None`; `by_id` miss→`None`; `liveBroadcastContent` `"live"`/`"upcoming"`→`None`, `"none"` passes; `concurrentViewers: 0`→`None` (`is not None` check, not truthiness); missing `actualEndTime`→`None`; elapsed under the floor→`None` **without calling the probe** (assert not called); probe `"was_live"`→returns the same object (`result is video`); probe `"post_live"`/`None`→`None`; a raising probe propagates (caller's handler owns fail-closed). Confirm RED.
- [ ] 2.3 Lift `_evaluate_finished_stream_candidate(video, video_id, by_id, guard_floor_minutes, cookies_file) -> dict | None` from base 374-418, immediately above `filter_finished_streams`. Replace with `candidate = _evaluate_finished_stream_candidate(...)` / `if candidate is not None: kept.append(candidate)`, **both statements inside the existing `try`**. Apply (a) x6 (`continue`→`return None`), (b) (`kept.append(video)`→`return video`; the `else` log branch unchanged), (g) (terminal `return None` appended to fall-through). No new helper contains a `try`/`except` — assert zero `Try` nodes in the helper (fail-closed propagation landmine).
- [ ] 2.4 Complexity re-measure: confirm `filter_finished_streams` ≤10 (predicted 7) and `_evaluate_finished_stream_candidate` ≤10 (predicted 8).
- [ ] 2.5 PR2 closing tasks, scoped to `congress_videos/modules/youtube/youtube_channel.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 2.6 Commit: `refactor(youtube-channel): lift finished-stream-candidate evaluation out of filter_finished_streams (#272)`. PR2 body: 📍 PR2, base = PR1 branch, `Refs #272`, revert-pairing note (with PR4).

## PR3 — `extract_session_date` (`congress_videos/modules/youtube/youtube_channel.py:985-1010, 1037-1047`)

- [ ] 3.1 Rebase onto PR2's tip. Baseline: confirm `extract_session_date` measures 14.
- [ ] 3.2 RED-first tests in `tests/congress_videos/modules/youtube/test_youtube_channel_extended.py`, class `TestParseAgendaDates`: unknown month→skipped, no entry; `31 DE FEBRERO`→`ValueError` caught, skipped; `original_index` counts accepted entries only (stays contiguous after a skip); explicit `DE 2024` beats `target_date_obj.year`; original `match` object kept. Literal fixture: text with 4 headers (valid, invalid-day, valid-with-year, unknown-month) → exactly 2 entries with `original_index` 0 and 1. Confirm RED.
- [ ] 3.3 RED-first tests in the same file, class `TestLocateTargetDateOffset`: **the falsy-valid trap** — target is the first date → `(0, entry, True)`, asserting `found_target is True` AND `offset == 0` together; not found → `(None, None, False)`; duplicate dates → first index wins (`break`); comparison uses `date_info["date"] == target_date_obj.date()` (a `date`, not `datetime`). Confirm RED.
- [ ] 3.4 Lift `_parse_agenda_dates(date_matches, spanish_months, target_date_obj) -> list[dict]` from base 985-1010 and `_locate_target_date_offset(sorted_dates, target_date_obj, target_date) -> tuple[int | None, dict | None, bool]` from base 1037-1047, both immediately above `extract_session_date`. Replace with `parsed_dates = _parse_agenda_dates(...)` and `date_offset, target_date_info, found_target = _locate_target_date_offset(...)`, applying (c) x2. Caller keeps `if not found_target:` verbatim — `found_target` stays the sole disambiguator for the legitimate offset-0 case.
- [ ] 3.5 Complexity re-measure: confirm `extract_session_date` ≤10 (predicted 9), `_parse_agenda_dates` ≤10 (predicted 4), `_locate_target_date_offset` ≤10 (predicted 3).
- [ ] 3.6 PR3 closing tasks, scoped to `congress_videos/modules/youtube/youtube_channel.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 3.7 Commit: `refactor(youtube-channel): lift agenda-date parsing and offset lookup out of extract_session_date (#272)`. PR3 body: 📍 PR3, base = PR2 branch, `Refs #272`, revert-pairing note (with PR4).

## PR4 — `extract_agenda_section` (`congress_videos/modules/youtube/youtube_channel.py:1144-1148, 1195, 1198-1233`) — characterization-first, budget-risk PR

- [ ] 4.1 Rebase onto PR3's tip. Baseline: confirm `extract_agenda_section` measures 17. Confirm zero existing coverage: `rg extract_agenda_section tests/` → 0 matches.
- [ ] 4.2 **Commit 1 (own commit, before any source change to `extract_agenda_section`; MUST NOT be squashed into commit 2 — the design requires this suite stay checked-out-able and green against untouched source)**: add class `TestExtractAgendaSection` to `tests/congress_videos/modules/youtube/test_youtube_channel_extended.py`, adjacent to `TestExtractSessionDate`, using the design's `AGENDA_TEXT`/`AGENDAS`/`SESSION_INFO` fixtures. Ten named tests: `test_extracts_section_between_target_header_and_next_header`, `test_last_date_section_runs_to_end_of_document`, `test_target_date_absent_returns_full_agenda_with_warning`, `test_no_parseable_date_headers_returns_full_agenda`, `test_invalid_and_unknown_month_headers_do_not_abort_the_scan`, `test_missing_agenda_for_video_id_yields_error_entry`, `test_empty_agenda_text_yields_error_entry`, `test_agenda_item_carrying_error_key_yields_error_entry`, `test_empty_inputs_return_zero_extracted`, `test_first_videos_target_date_must_parse`. Run and confirm all ten green against untouched pre-refactor source.
- [ ] 4.3 RED-first tests in the same file, class `TestFindAgendaForVideo`: first match wins on duplicated `video_id`; `None` when absent; an agenda item without a `video_id` key raises `KeyError` (pre-existing, direct subscript). Confirm RED.
- [ ] 4.4 RED-first tests in the same file, class `TestLocateTargetSection`: `None` when no header matches the target; boundary is the smallest start greater than this match's (not list order); target last → section runs to EOF; result is `.strip()`ped; unknown month and invalid date skip without aborting; `test_returns_empty_string_when_slice_is_whitespace_only` — `agenda_text` passed as `"          "` → returns `""` (the caller's `if target_section:` then treats it as not-found). Confirm RED.
- [ ] 4.5 **Commit 2 (lift + prune, after commit 1 lands)**: lift `_find_agenda_for_video(agendas, video_id) -> dict | None` from base 1144-1148 and `_locate_target_section(agenda_text, date_matches, target_date_dt, spanish_months) -> str | None` from base 1195 + 1198-1233, both immediately above `extract_agenda_section`. Replace with `agenda_item = _find_agenda_for_video(agendas, video_id)` and `target_section = _locate_target_section(...)`, applying (c) x2 and (f) (`target_section = None` at 1195 relocated across 1196, mechanically asserted no-read/no-write crossing). Line 1235 `if target_section:` stays **outside** every lifted range, byte-identical — never rewritten to `is not None`.
- [ ] 4.6 Complexity re-measure: confirm `extract_agenda_section` ≤10 (predicted 8), `_find_agenda_for_video` ≤10 (predicted 3), `_locate_target_section` ≤10 (predicted 8).
- [ ] 4.7 In commit 2: hidden-regression check `uvx ruff check --select C901 --no-cache --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/modules/youtube/youtube_channel.py` (after rebasing onto `origin/dev`) → zero offenders across all four `youtube_channel.py` functions (PR1-4 must all be merged first). Edit `pyproject.toml` line 127 `["B007","C901","F841","SIM102"]` → `["B007","F841","SIM102"]`; decrement `EXPECTED_C901_FILE_COUNT` 7→6 in `tests/test_ruff_config.py`. Same commit as 4.5.
- [ ] 4.8 **Budget check**: `git diff --shortstat` against PR4 base. If total additions+deletions exceeds 400, apply the pre-approved contingency: **4a** = commit 1 (characterization tests) only, no source change, no prune, function still at 17 and still masked, counter unmoved (state this explicitly in the PR body); **4b** = commit 2 (both helpers + prune + counter 7→6). Re-run 4.6-4.7 scoped to 4b if split is triggered.
- [ ] 4.9 PR4 (or 4a/4b) closing tasks, scoped to `congress_videos/modules/youtube/youtube_channel.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 4.10 Commits: `test(youtube-channel): add characterization tests for extract_agenda_section (#272)`, `refactor(youtube-channel): lift agenda lookup and section location, drop C901 token (#272)`. PR4 body (or 4a/4b): 📍 PR4, base = PR3 branch, `Refs #272`, measured line count, note PR1-4 only revertible together/in order.

## PR5 — `_dedup_overlapping_chapters` (`congress_videos/modules/youtube/download.py:1079-1145`)

- [ ] 5.1 Rebase onto PR4's tip. Baseline: confirm `_dedup_overlapping_chapters` measures 14.
- [ ] 5.2 RED-first tests in `tests/congress_videos/modules/youtube/test_download.py`, class `TestChapterStartEndSecs`: missing key → default `"00:00:00"` → `0.0`; unparseable `"abc"` → `ValueError` swallowed → `0.0` (never raises), for both `_chapter_start_secs` and `_chapter_end_secs`. Confirm RED.
- [ ] 5.3 RED-first tests in the same file, class `TestMarkOverlappingChapters`: mutates `keep` in place, returns `None`; **the `<=` boundary** — unsorted `[a(00:00:00-00:10:00), b(00:10:00-00:20:00), c(00:05:00-00:07:00)]` → `keep == [True, True, True]` because `b` touches `a` at `overlap == 0.0` and `break`s before `c` is compared (would differ under `<`); `min_dur <= 0.0` → `continue` (degenerate chapter skipped, not discarded); when narrower is `i`, `keep[i] = False` and the row breaks. Confirm RED.
- [ ] 5.4 Lift `_chapter_start_secs(ch: dict) -> float` from base 1079-1083, `_chapter_end_secs(ch: dict) -> float` from base 1085-1089, and `_mark_overlapping_chapters(sorted_chapters, keep) -> None` from base 1096-1145, all immediately above `_dedup_overlapping_chapters`. Replace with `sorted_chapters = sorted(chapters, key=_chapter_start_secs)` and `_mark_overlapping_chapters(sorted_chapters, keep)`, applying (d) x2 (Name-alias substitution: `_start_secs`→`_chapter_start_secs`, `_end_secs`→`_chapter_end_secs`). `overlap <= 0.0` (base 1110) MUST appear in the shipped dump as `Compare(ops=[LtE()])` — keep `<=`, never rewrite to `<`.
- [ ] 5.5 Complexity re-measure: confirm `_dedup_overlapping_chapters` ≤10 (predicted 2), `_chapter_start_secs`/`_chapter_end_secs` ≤10 (predicted 2 each), `_mark_overlapping_chapters` ≤10 (predicted 9 — if measured at 10+, apply the pre-approved contingency: lift the inner `for j` loop 1099-1145 into `_scan_overlaps_from(sorted_chapters, keep, i) -> None`, both `break`s stay inner-loop `break`s, zero normalization).
- [ ] 5.6 PR5 closing tasks, scoped to `congress_videos/modules/youtube/download.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 5.7 Commit: `refactor(download): lift chapter-boundary accessors and overlap marking out of _dedup_overlapping_chapters (#272)`. PR5 body: 📍 PR5, base = PR4 branch, `Refs #272`, revert-pairing note (with PR7).

## PR6 — `identify_interesting_chapters` (`congress_videos/modules/youtube/download.py:1461-1549`)

- [ ] 6.1 Rebase onto PR5's tip. Baseline: confirm `identify_interesting_chapters` measures 12.
- [ ] 6.2 RED-first tests in `tests/congress_videos/modules/youtube/test_download.py`, class `TestFindSrtChunksForVideo`: `None`/`{}`/missing `videos` → `[]`; first matching video wins (`break`); a matched video without `chunks` → `[]`; items lacking `video_id` do not raise (`.get`). Confirm RED.
- [ ] 6.3 RED-first tests in the same file, class `TestCollectChunkChapters`: `_find_srt_chunk` returning `""` → `{"chunk_number": n, "error": "No SRT content available"}` (intentional falsy check at base 1490 — do NOT "fix" to `is None`); `chunk_duration == max_optimal_duration` → whole-chunk path, `reason == "optimal duration"` (`<=`, not `<`); below `min_chapter_duration` → `reason == "too short"`, still whole-chunk; above max → delegates to `_analyze_single_chunk` with positional args in base order; title truncated at 100 chars; missing `duration_minutes` → `0` → too short. Confirm RED.
- [ ] 6.4 Lift `_find_srt_chunks_for_video(chunked_srt_data, video_id) -> list[dict]` from base 1461-1466 and `_collect_chunk_chapters(summarized_chunks, srt_chunks, min_chapter_duration, max_optimal_duration) -> list[dict]` from base 1477 + 1479 + 1482-1549, both immediately above `identify_interesting_chapters`. Replace with `srt_chunks = _find_srt_chunks_for_video(chunked_srt_data, video_id)` and `chunks_with_chapters = _collect_chunk_chapters(...)`, applying (c) x2. `_build_srt_chunk_index` (base 1479) moves inside `_collect_chunk_chapters` so the block stays contiguous; both `continue`s move verbatim.
- [ ] 6.5 Complexity re-measure: confirm `identify_interesting_chapters` ≤10 (predicted 6), `_find_srt_chunks_for_video` ≤10 (predicted 4), `_collect_chunk_chapters` ≤10 (predicted 4).
- [ ] 6.6 PR6 closing tasks, scoped to `congress_videos/modules/youtube/download.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 6.7 Commit: `refactor(download): lift SRT-chunk lookup and chapter collection out of identify_interesting_chapters (#272)`. PR6 body: 📍 PR6, base = PR5 branch, `Refs #272`, revert-pairing note (with PR7).

## PR7 — `_analyze_single_chunk` (`congress_videos/modules/youtube/download.py:1291-1340`) + `download.py` C901 prune + counter 6→5

- [ ] 7.1 Rebase onto PR6's tip. Baseline: confirm `_analyze_single_chunk` measures 15.
- [ ] 7.2 RED-first tests in `tests/congress_videos/modules/youtube/test_download.py`, class `TestIdentifyChaptersForChunk`: `completion["error"]` truthy → `RuntimeError` **propagates out of the helper** (outer handler owns the fallback); `completion["data"] is None` → `[]`; under `LARGE_SRT_THRESHOLD` → exactly one `cached_json_completion` call with the full `srt_content` and `model=LLM_CHEAP`; over threshold → capture the `identify_fn` kwarg, invoke it with a synthetic window, assert the resulting `user_prompt` contains the same `summary_text` (`"Chunk 7 (00:00:00 - 01:00:00)"`, speaker line, `"Topics: a, b"`, `"Summary: …"`) — this is the closure-capture proof, mandatory; optional sections absent when their keys are missing/empty. Confirm RED.
- [ ] 7.3 Lift `_identify_chapters_for_chunk(chunk_number, summary_chunk, srt_content, chunk_duration, system_prompt, user_prompt_template) -> list[dict]` from base 1291-1340, immediately above `_analyze_single_chunk`. Move the `_identify_window` closure and its call into `map_reduce_identify_chapters` together as one atomic unit inside the helper; `summary_text` stays entirely inside the helper's scope (no cross-boundary capture). Replace with `interesting_chapters = _identify_chapters_for_chunk(chunk_number, summary_chunk, srt_content, chunk_duration, CHAPTER_IDENTIFICATION_SYSTEM_PROMPT, CHAPTER_IDENTIFICATION_USER_PROMPT_TEMPLATE)`, applying (c) and (d) x2 (prompt constants → `system_prompt`/`user_prompt_template` parameters). The `from congress_videos.config.ai_prompts import (...)` at base 1281-1284 **stays in the caller** — do NOT move it into the helper (would put an `ImportError` inside the `try`'s reach at 1289, silently converting a hard failure into a whole-chunk fallback). `interesting_chapters` stays live in the outer scope for the `is_single_chapter` comparison at 1381 — the helper does not consume or shadow it. `import json` (1279) and the `_flatten_speakers` import (1285) stay untouched (still used at 1403/1372).
- [ ] 7.4 Complexity re-measure: confirm `_analyze_single_chunk` ≤10 (predicted 8), `_identify_chapters_for_chunk` ≤10 (predicted 8).
- [ ] 7.5 Hidden-regression check `uvx ruff check --select C901 --no-cache --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/modules/youtube/download.py` (after rebasing onto `origin/dev`) → zero offenders across all three `download.py` functions (PR5-7 all merged first). Edit `pyproject.toml` line 126 `["B905","C901","SIM103","SIM108"]` → `["B905","SIM103","SIM108"]`; decrement `EXPECTED_C901_FILE_COUNT` 6→5. Same commit as 7.3.
- [ ] 7.6 PR7 closing tasks, scoped to `congress_videos/modules/youtube/download.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 7.7 Commit: `refactor(download): lift chapter-identification helper, drop C901 token (#272)`. PR7 body: 📍 PR7, base = PR6 branch, `Refs #272`, note PR5-7 only revertible together/in order.

## PR8 — `download_youtube_subtitles` (`utils/youtube_downloader.py:883-933`)

- [ ] 8.1 Rebase onto PR7's tip. Baseline: confirm `download_youtube_subtitles` measures 11.
- [ ] 8.2 RED-first tests in `tests/utils/test_youtube_downloader.py`, class `TestDownloadSubtitleFiles`: per-language exception → `continue`, next language attempted, no raise; `break` after the first language that yields files (later languages never attempted); `is_auto` pinned on both halves of `"auto" in srt_file.name.lower() or lang == "auto"` — `("v1_es-AUTO.srt", lang="es") → True`, `("v1_zz.srt", lang="auto") → True`, `("v1_es.srt", lang="es") → False`; all languages failing → `[]`; one entry per file in the glob, all sharing the same `language`. Confirm RED.
- [ ] 8.3 Lift `_download_subtitle_files(youtube_url, video_id, output_dir, languages) -> list[dict]` from base 883-933, immediately above `download_youtube_subtitles`. Replace with `downloaded_files = _download_subtitle_files(youtube_url, video_id, output_dir, languages)`, applying (c). The 948-973 merge block stays untouched, unmoved.
- [ ] 8.4 Complexity re-measure: confirm `download_youtube_subtitles` ≤10 (predicted 7), `_download_subtitle_files` ≤10 (predicted 5).
- [ ] 8.5 PR8 closing tasks, scoped to `utils/youtube_downloader.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 8.6 Commit: `refactor(youtube-downloader): lift subtitle-file download out of download_youtube_subtitles (#272)`. PR8 body: 📍 PR8, base = PR7 branch, `Refs #272`, revert-pairing note (with PR9).

## PR9 — `download_with_pytubefix` + `download_youtube_video_for_upload` (`utils/youtube_downloader.py`) + C901 prune + counter 5→4

- [ ] 9.1 Rebase onto PR8's tip. Baseline: confirm `download_with_pytubefix` measures 11 and `download_youtube_video_for_upload` measures 11.
- [ ] 9.2 RED-first tests in `tests/utils/test_youtube_downloader.py`, class `TestLogAvailableStreams`: returns `None`; logs at most the first 15 streams while the header reports the full count (20 in → 1 header + 15 detail lines); a stream with `resolution=None` logs `"audio"`. Confirm RED.
- [ ] 9.3 RED-first tests in the same file, class `TestSelectVideoStream`: prefers mp4 adaptive `>= min_resolution`; falls back to any mp4 adaptive; then to any adaptive; `None` when all three yield `None`; a stream with `resolution=None` excluded by the `s.resolution and …` short-circuit (no `TypeError`). Confirm RED.
- [ ] 9.4 Lift `_log_available_streams(yt) -> None` from base 140-146 and `_select_video_stream(yt, min_resolution)` from base 154-172, both immediately above `download_with_pytubefix`. Replace with `_log_available_streams(yt)` and `video_stream = _select_video_stream(yt, min_resolution)`, applying (c). Lines 174-275 (subprocess merge, both cleanup paths, mid-function `return` at 238) stay **completely untouched** — proof step 4 asserts the outer's statement list from 174 onward is byte-identical to base.
- [ ] 9.5 RED-first tests in the same file, class `TestCheckLiveStatusGuard`: `guard_live_status=False` → `None` **without probing** (assert not called); probe `None` (error) → `None`, non-blocking; `"was_live"`/`"not_live"` → `None`; `"post_live"` → skip dict with `success False`, `skipped True`, exact `f"live_status {status!r} not ready — skipped download"` (pin `!r` quoting). Confirm RED.
- [ ] 9.6 RED-first tests in the same file, class `TestTryPytubefixDownload`: `use_pytubefix_first=False` → `None` **without calling** `download_with_pytubefix`; success → returns that exact dict object; a raising `_warn_if_not_h264` is swallowed and the result still returned; `success False` → `None` (falls through to yt-dlp). Confirm RED.
- [ ] 9.7 Lift `_check_live_status_guard(youtube_url, cookies_file, guard_live_status) -> dict | None` from base 329-341 and `_try_pytubefix_download(youtube_url, output_dir, min_resolution, use_pytubefix_first) -> dict | None` from base 348-359, both immediately above `download_youtube_video_for_upload`. Replace with `skip_result = _check_live_status_guard(...)` / `if skip_result is not None: return skip_result` and `pytubefix_result = _try_pytubefix_download(...)` / `if pytubefix_result is not None: return pytubefix_result`, applying (e') x2 (sentinel re-check, forward form) and (g) x2 (terminal `return None` appended). The outer `Try` at base 405 keeps handler order `[yt_dlp.utils.DownloadError, Exception]`, body unchanged — nothing extracted from inside it.
- [ ] 9.8 Complexity re-measure: confirm `download_with_pytubefix` ≤10 (predicted 8), `_log_available_streams` ≤10 (predicted 2), `_select_video_stream` ≤10 (predicted 3), `download_youtube_video_for_upload` ≤10 (predicted 8), `_check_live_status_guard` ≤10 (predicted 3), `_try_pytubefix_download` ≤10 (predicted 4).
- [ ] 9.9 Hidden-regression check `uvx ruff check --select C901 --no-cache --config 'lint.per-file-ignores = {}' --output-format concise utils/youtube_downloader.py` (after rebasing onto `origin/dev`) → zero offenders (PR8-9 both merged first). Edit `pyproject.toml` line 162 `["C901","F841"]` → `["F841"]`; decrement `EXPECTED_C901_FILE_COUNT` 5→4. Same commit as 9.4/9.7.
- [ ] 9.10 **Budget check**: `git diff --shortstat` against PR9 base. If total additions+deletions exceeds 400, apply the pre-approved contingency: **9a** = `download_with_pytubefix` lift only (9.2-9.4), no token drop; **9b** = `download_youtube_video_for_upload` lift (9.5-9.7) + prune + counter 5→4 (9.9). Re-run 9.8 scoped to each if split is triggered.
- [ ] 9.11 PR9 (or 9a/9b) closing tasks, scoped to `utils/youtube_downloader.py`; `git diff --shortstat` ≤400; existing-test-diff additions-only check.
- [ ] 9.12 Commits: `refactor(youtube-downloader): lift stream logging and selection out of download_with_pytubefix (#272)`, `refactor(youtube-downloader): lift live-status guard and pytubefix attempt, drop C901 token (#272)`. PR9 body (or 9a/9b): 📍 PR9, base = PR8 branch, `Refs #272`, note PR8-9 only revertible together/in order.

## PR10 — Release PR `dev -> main`

- [ ] 10.1 With all nine PRs merged to `dev` at the stack tip: no `Closes #272` in any PR body across the stack (`Refs #272` only) — issue #272 stays open with 4 entries remaining.
- [ ] 10.2 Confirm `pyproject.toml` carries exactly 4 `"C901"` entries and `EXPECTED_C901_FILE_COUNT == 4` in `tests/test_ruff_config.py` (both benchmark `server.py`, `vad_helpers.py`, `reap_shorts_uploader_dag.py` remain); confirm `test_every_code_list_is_sorted_and_deduped` passes.
- [ ] 10.3 Open the release PR `dev -> main`; body lists the ladder 7→6→5→4 and every function's before→after complexity from the per-lift table.

## Final Verification Phase

- [ ] 11.1 Full-suite run at the final `dev` tip: `uv run pytest` with default addopts and coverage gate. Confirm green with **>= 5274 passed, 34 skipped** and coverage `>= 80%` (base + this slice's new tests, never fewer than base).
- [ ] 11.2 `git diff origin/main..HEAD -- tests/` (pre-slice base `7e3e689`) shows additions only — zero pre-existing assertions edited across the whole slice.
- [ ] 11.3 Run `bash scripts/test-airflow-e2e.sh` at the final tip (the slice touches `congress_videos/**` and `utils/**`). If Docker is unavailable it reports `unavailable` (not a failure) — record that explicitly and note a NAS `airflow dags list-import-errors` check is owed on **both** dev and prod schedulers after `git_sync_dag`, before merging PR10 to `main`.
- [ ] 11.4 After merge to `dev`, trigger `git_sync_dag` on each NAS scheduler and confirm `airflow dags list-import-errors` is empty on both. Repeat after the release PR merges to `main`.
- [ ] 11.5 Post the slice-5 report comment on issue #272: shipped functions with complexity before→after (`get_video_details` 11→9, `filter_finished_streams` 13→7, `extract_session_date` 14→9, `extract_agenda_section` 17→8, `_dedup_overlapping_chapters` 14→2, `identify_interesting_chapters` 12→6, `_analyze_single_chunk` 15→8, `download_youtube_subtitles` 11→7, `download_with_pytubefix` 11→8, `download_youtube_video_for_upload` 11→8); the counter ladder 7→6→5→4; and every deferral with its reason.
- [ ] 11.6 Record the deferred backlog in the issue #272 comment: `create_app` (both benchmark servers), `_default_model_loader`, `trim_turn_silence_with_vad`, `_generate_metadata`, `build_shorts_metadata_context` — untouched, sized for a future slice.
- [ ] 11.7 **File a new GitHub issue** for the latent production bug in `get_video_details`: a video whose `contentDetails.duration` does not match `PT…` (e.g. `P0D`) either reuses the previous video's `hours`/`minutes`/`seconds` in `duration_formatted`, or raises `NameError` if it is the first such video. Confirmed independently against source (base 513-527) and pinned by this slice's `TestGetVideoDetailsDurationLeak` characterization tests (PR1). Out of scope for this behaviour-preserving refactor — do NOT fix it inside this slice. Link the issue from #272's slice-5 report.
- [ ] 11.8 **File or update a GitHub issue** for the `spanish_months`/`date_pattern` duplication between `extract_agenda_section` and `extract_session_date` (both in `youtube_channel.py`) — untouched by this slice per the spec's "Deferred functions and files stay untouched" requirement. Link the issue from #272's slice-5 report.
