# Tasks: C901 Backlog Slice 4 (issue #272)

Base: worktree `/home/alozur/src/github.com/alozur/airflow-dags-wt-272-s4` @ `origin/main 1aa5681`
(`dev == main` at start). Branches `refactor/272-c901-slice-4-pr1` .. `-pr6`, stacked to `dev`.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | PR1 ~220 · PR2 ~280 · PR3 ~330 ⚠ · PR4 ~160 · PR5 ~245 · PR6 ~145 (total ~1380) |
| 400-line budget risk | Medium — every PR is under 400; PR3 is the tight one (~70 lines headroom) |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3 (→ PR3a/PR3b contingency) → PR4 → PR5 → PR6 → release PR |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Medium

### Suggested Work Units

| Unit | Goal | PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | Prune `reap_clip_preparer_dag.py` entry; lift `is_procedural_turn`/`extract_announcement`; drop `C901` token | PR1 | `uv run pytest -o addopts= tests/congress_videos/modules/test_speaker_turns_procedural.py tests/congress_videos/modules/test_speaker_turns.py` | N/A — pure helpers, no DAG/runtime surface change | `git revert` PR1 merge commit; independent, leads the stack |
| 2 | Lift `normalize_chapter_speakers`; prune entry | PR2 | `uv run pytest -o addopts= tests/congress_videos/modules/test_speaker_normalization.py` | N/A — pure DB-cursor helper, no scheduling surface | `git revert` PR2; independent |
| 3 | Lift `_resolve_speaker_inner`; prune entry (split PR3a/PR3b if >400) | PR3 | `uv run pytest -o addopts= tests/congress_videos/modules/test_speaker_resolution.py` | N/A — pure prompt/validation helpers | `git revert` PR3 (or PR3a+PR3b together); independent |
| 4 | Characterization + direct tests for `derive_candidate_intervals`, then lift; prune entry | PR4 | `uv run pytest -o addopts= tests/benchmarks/test_pyannote_diarization_candidate_intervals.py` | N/A — offline benchmark module, no Airflow import | `git revert` PR4; independent |
| 5 | Lift `_resolve_qa_winner`/`_persist_turn_resolution` out of `_prepare_turns_callable` (19→11, no token drop) | PR5 | `uv run pytest -o addopts= tests/congress_videos/test_speaker_turn_prepare_dag.py` | DagBag-parsed module: `uv run python -c "from airflow.models import DagBag; b=DagBag(dag_folder='congress_videos', include_examples=False); assert not b.import_errors, b.import_errors"` at tip | `git revert` PR5 **only together with PR6** (or PR6 first) — see PR6 pairing note |
| 6 | Lift `_prepare_turn_artifacts` (11→9); drop `C901` token | PR6 | `uv run pytest -o addopts= tests/congress_videos/test_speaker_turn_prepare_dag.py` | Same DagBag import check at tip; full `bash scripts/test-airflow-e2e.sh` at the slice's final tip | `git revert` PR6 (must accompany or follow PR5 revert); final tip of the stack |

## PR1 — free prune + `is_procedural_turn` + `extract_announcement` (`congress_videos/modules/speaker_turns.py`, `congress_videos/reap_clip_preparer_dag.py`)

- [x] 1.1 Hidden-regression pre-check: `uvx ruff check --select C901 --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/reap_clip_preparer_dag.py congress_videos/modules/speaker_turns.py`; re-run after rebasing onto `origin/dev`.
- [x] 1.2 Free prune: delete the `reap_clip_preparer_dag.py` `pyproject.toml` entry (0 violations, no source edit). Commit with `EXPECTED_C901_FILE_COUNT` 13→12 in `tests/test_ruff_config.py`, same commit.
- [x] 1.3 RED-first test for `_collect_pattern_spans` in `tests/congress_videos/modules/test_speaker_turns_procedural.py`: dedup of `matched_names` by pattern name vs. one `spans` entry per match, in-place mutation of both caller lists, returns `None`. Confirm it fails (helper does not exist yet).
- [x] 1.4 Lift `_collect_pattern_spans(patterns, normalized, spans, matched_names) -> None` immediately above `is_procedural_turn` in `congress_videos/modules/speaker_turns.py`, from base lines 337-341 and 347-351 (byte-identical `for` blocks); replace both with one call per pattern tuple, applying normalization (d) (parameter-alias substitution `patterns`→`PROCEDURAL_PATTERNS`/`PROCEDURAL_FILLER_PATTERNS`, checked against **both** base sites).
- [x] 1.5 RED-first tests for `_first_named_announcement` and `_first_phrase_announcement` in `tests/congress_videos/modules/test_speaker_turns.py`: first named match wins in input order; `_RE_SU_SENORIA` checked before `_RE_GRACIAS_SENORIA` with no `elif`; gracias-only block yields `(None, True)`; `None` on no match in either. Confirm RED before the lift.
- [x] 1.6 Lift `_first_named_announcement(sorted_blocks)` (base 418+421-429) and `_first_phrase_announcement(sorted_blocks)` (base 419+434-443) immediately above `extract_announcement`; replace with `best_named = _first_named_announcement(sorted_blocks)` / `best_phrase = _first_phrase_announcement(sorted_blocks)`, applying (c) appended trailing `Return` and (f) inert-init relocation for `best_phrase` (mechanically assert the no-read/no-write crossing property).
- [x] 1.7 Run the scratch AST-equality proof (`ast_check_s4.py`, session scratchpad, never versioned) for both lifts; capture the `OK` lines for the PR body.
- [x] 1.8 Complexity re-measure: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' congress_videos/modules/speaker_turns.py`; confirm `is_procedural_turn` and `extract_announcement` both ≤10 (predicted 6 and 4).
- [x] 1.9 Re-run the hidden-regression check on `speaker_turns.py` immediately before the token drop (multi-offender entry rule). In the same commit as the second lift, edit the `pyproject.toml` entry `["C901","F841","SIM102","SIM108"]` → `["F841","SIM102","SIM108"]` and decrement `EXPECTED_C901_FILE_COUNT` 12→11 in `tests/test_ruff_config.py`.
- [x] 1.10 `uvx ruff check .` + `uvx ruff format --check .` + focused `uv run pytest -o addopts= tests/congress_videos/modules/test_speaker_turns_procedural.py tests/congress_videos/modules/test_speaker_turns.py` at the PR1 tip.
- [x] 1.11 Commit messages: `refactor(speaker-turns): lift pattern-span collection out of is_procedural_turn (#272)`, `refactor(speaker-turns): lift announcement helpers and drop C901 token (#272)` (+ the free-prune commit). PR1 body: dependency diagram with 📍 PR1, base `dev`, `Refs #272`, no `Closes`.

## PR2 — `normalize_chapter_speakers` (`congress_videos/modules/speaker_normalization.py`)

- [x] 2.1 Rebase onto PR1's tip; hidden-regression pre-check: `uvx ruff check --select C901 --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/modules/speaker_normalization.py` against `origin/dev`.
- [x] 2.2 RED-first tests in `tests/congress_videos/modules/test_speaker_normalization.py`: `_apply_institutional_role_corrections` — `resolved is None`/`role_name == raw` both skip without a cache write, `participant_normalized_name` is `None` when not a participant, slug written only when currently `None` and `is_participant`, `result` mutated in place; `_apply_roster_resolution_step` — empty roster still calls the resolver, only the first `MAX_MENTIONS_PER_CALL` mentions are sent but all `dirty_names` iterate (over-cap → `no_match`), slug is the first accepted match in input order. Confirm both RED.
- [x] 2.3 Lift `_apply_institutional_role_corrections(cursor, chapter_id, speakers, key_speakers, timeline, session_date, result) -> None` from base 247-279 (Step-0 `for` body) and `_apply_roster_resolution_step(cursor, chapter_id, dirty_names, result) -> None` from base 287-339 (`if dirty_names:` body), both immediately above `normalize_chapter_speakers`, no declared normalization. Replace with one call inside the kept `if session_date is not None:` guard and one call inside the kept `if dirty_names:` guard; `result` and `cursor` pass through as the same live objects (never copied).
- [x] 2.4 Run the AST-equality proof for both lifts; capture `OK` lines.
- [x] 2.5 Complexity re-measure: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' congress_videos/modules/speaker_normalization.py`; confirm `normalize_chapter_speakers` ≤10 (predicted 6).
- [x] 2.6 Same commit: delete the `speaker_normalization.py` `pyproject.toml` entry (full prune) and decrement `EXPECTED_C901_FILE_COUNT` 11→10 in `tests/test_ruff_config.py`.
- [x] 2.7 `uvx ruff check .` + `uvx ruff format --check .` + focused `uv run pytest -o addopts= tests/congress_videos/modules/test_speaker_normalization.py` at the PR2 tip.
- [x] 2.8 Commit: `refactor(speaker-normalization): lift chapter-speaker correction steps out of normalize_chapter_speakers (#272)`. PR2 body: 📍 PR2, base = PR1 branch, `Refs #272`.

## PR3 — `_resolve_speaker_inner` (`congress_videos/modules/speaker_resolution.py`) — budget-risk PR

- [x] 3.1 Rebase onto PR2's tip; hidden-regression pre-check: `uvx ruff check --select C901 --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/modules/speaker_resolution.py` against `origin/dev`.
- [x] 3.2 RED-first tests in `tests/congress_videos/modules/test_speaker_resolution.py`: `_build_resolution_user_prompt` — wide template only when `QA_WIDE_CONTEXT_ENABLED and turn_type=='qa' and chapter_span is not None`, unparseable span on a qa turn warns and falls back narrow, `None` only from the announcement pre-gate, gate reads `chapter_text` (wide) vs `combined_text` (narrow); `_validate_completion_response` — truthy `error` or empty `data` → `None`, slug not in `valid_slugs` → `None`, string confidence coerced by `float()`, confidence exactly at `SPEAKER_RESOLUTION_MIN_CONFIDENCE` passes (`>=`), `evidence` defaults to `""`. Confirm both RED.
- [x] 3.3 Lift `_build_resolution_user_prompt(turn, participants, all_blocks, chapter_span, chapter_start_seconds, region_end, intro_text, turn_text, combined_text) -> str | None` from base 385-428 and `_validate_completion_response(turn, response, valid_slugs, region_blocks) -> dict | None` from base 442-502 (tail lift, includes the function's own `return {...}`), both immediately above `_resolve_speaker_inner`. Replace with `user_prompt = _build_resolution_user_prompt(...)` + `if user_prompt is None: return None` (normalization (e), abort-sentinel re-check) and `return _validate_completion_response(...)` (normalization (c)). Keep `chapter_id or 0` (line 318) and `group_start_seconds is not None` (342-343) untouched in the caller — both stay outside every lifted range.
- [x] 3.4 Run the AST-equality proof for both lifts, including the (e) replacement-statement check; capture `OK` lines.
- [x] 3.5 Complexity re-measure: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' congress_videos/modules/speaker_resolution.py`; confirm `_resolve_speaker_inner` ≤10 (predicted 6).
- [x] 3.6 Same commit: delete the `speaker_resolution.py` `pyproject.toml` entry and decrement `EXPECTED_C901_FILE_COUNT` 10→9 in `tests/test_ruff_config.py`.
- [x] 3.7 **Budget check**: `git diff --shortstat` against the PR3 base (`origin/dev` after PR2 merges). If total additions+deletions exceeds 400, split per the pre-approved contingency: **PR3a** = `_build_resolution_user_prompt` lift + its tests only, no `pyproject.toml` edit (function still at 11, still masked); **PR3b** = `_validate_completion_response` lift + its tests + the prune + the counter decrement 10→9. Re-run steps 3.1-3.6 scoped to each sub-PR if split is triggered. **Triggered**: single-commit measurement was 504 changed lines (>400); split into PR3a (271 lines) + PR3b (232 lines), both under budget.
- [x] 3.8 `uvx ruff check .` + `uvx ruff format --check .` + focused `uv run pytest -o addopts= tests/congress_videos/modules/test_speaker_resolution.py` at the PR3 (or PR3a/PR3b) tip.
- [x] 3.9 Commit: `refactor(speaker-resolution): lift prompt-building and response-validation out of _resolve_speaker_inner (#272)` (or two commits if split). PR body: 📍 PR3 (or PR3a/PR3b), base = PR2 branch, `Refs #272`, note the measured line count from 3.7. Two commits: `f09becf` (PR3a) and `d74a395` (PR3b).

## PR4 — `derive_candidate_intervals` (`benchmarks/pyannote_diarization/candidate_intervals.py`) — characterization tests first

- [x] 4.1 Rebase onto PR3's tip; hidden-regression pre-check: `uvx ruff check --select C901 --config 'lint.per-file-ignores = {}' --output-format concise benchmarks/pyannote_diarization/candidate_intervals.py` against `origin/dev`.
- [x] 4.2 **Commit 1 (separate, before any lift)**: add direct unit tests to `tests/benchmarks/test_pyannote_diarization_candidate_intervals.py` for `derive_candidate_intervals` itself, pinned from literals already proven by the existing CLI-subprocess tests: `duration=20.0, turns=[(-2,4),(3,6),(10,12)], min_gap=3.0 → [(6,10),(12,20)]` (negative clamp, overlap merge, interior + tail gap, `NO_DIARIZED_SPEECH` label); `duration=10.0, turns=[(4,6)], min_gap=5 → []` (both leading and tail gaps rejected under threshold); `duration=10.0, turns=[(4,2)]` → `SummaryValidationError("...end_seconds must not precede start_seconds")`. Confirm all three pass green against **untouched** pre-refactor source (characterization, not RED).
- [x] 4.3 RED-first tests in the same file for `_merge_active_intervals` (sorts internally, touching intervals merge, fully-contained interval does not shrink the merged end) and `_intervals_to_gaps` (`>=` on `min_gap_seconds`, tail gap = `duration - cursor`, 6-decimal rounding, `min_gap_seconds=0.0` emits zero-length gaps). Confirm RED before the lift.
- [x] 4.4 **Commit 2 (lift + prune)**: lift `_merge_active_intervals(active_intervals) -> list[list[float]]` from base 63-68 and `_intervals_to_gaps(merged_intervals, duration, min_gap_seconds) -> list[dict[str, object]]` from base 70-90 (tail lift, includes `return gaps`), both immediately above `derive_candidate_intervals`. Replace with `merged_intervals = _merge_active_intervals(active_intervals)` then `return _intervals_to_gaps(merged_intervals, duration, min_gap_seconds)`, applying normalization (c).
- [x] 4.5 Run the AST-equality proof for both lifts; capture `OK` lines.
- [x] 4.6 Complexity re-measure: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' benchmarks/pyannote_diarization/candidate_intervals.py`; confirm `derive_candidate_intervals` ≤10 (predicted 7).
- [x] 4.7 In commit 2: delete the `candidate_intervals.py` `pyproject.toml` entry and decrement `EXPECTED_C901_FILE_COUNT` 9→8 in `tests/test_ruff_config.py`.
- [x] 4.8 `uvx ruff check .` + `uvx ruff format --check .` + focused `uv run pytest -o addopts= tests/benchmarks/test_pyannote_diarization_candidate_intervals.py` at the PR4 tip.
- [x] 4.9 Commits: `test(candidate-intervals): pin derive_candidate_intervals behavior from CLI-proven literals (#272)`, `refactor(candidate-intervals): lift merge and gap-derivation helpers out of derive_candidate_intervals (#272)`. PR4 body: 📍 PR4, base = PR3 (or PR3b) branch, `Refs #272`.

## PR5 — `_prepare_turns_callable` AI-resolution helpers (`congress_videos/speaker_turn_prepare_dag.py`, part 1 of 2)

- [x] 5.1 Rebase onto PR4's tip; hidden-regression pre-check: `uvx ruff check --select C901 --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/speaker_turn_prepare_dag.py` against `origin/dev`.
- [x] 5.2 RED-first tests in `tests/congress_videos/test_speaker_turn_prepare_dag.py`: `_resolve_qa_winner` — wide re-pass only when `promote_signal and QA_WIDE_CONTEXT_ENABLED`, a raising wide call falls back to primary without propagating, a wide candidate rejected by crosscheck leaves `winner_verdict is None` so the primary is crosschecked afterward, `wide_slug` is reported even when the wide candidate loses, `turn` is never mutated; `_persist_turn_resolution` — verdict `"reject"` withholds both the DB write and the in-memory `resolved_name` patch, promotion is sticky on `promote_signal` and never re-evaluated against the winner, `promoted` stays `False` when `winner_name` is falsy even if `promote_signal` is `True`. Confirm both RED.
- [x] 5.3 Lift `_resolve_qa_winner(turn, participants, turn_id, primary, primary_name, promote_signal, mentions) -> tuple[dict, str | None, str | None, str | None]` from base 328-364 and `_persist_turn_resolution(db, turn, turn_id, output_path, winner, winner_name, winner_verdict, promote_signal) -> bool` from base 366-402, both immediately above `_prepare_turns_callable`. Replace with `winner, winner_name, winner_verdict, wide_slug = _resolve_qa_winner(...)` then `promoted = _persist_turn_resolution(...)` (normalization (c) on both). Keep the audit log at 404-418 in the caller reading `wide_slug`/`promoted`; the outer `except Exception` at 431 keeps catching both helpers unchanged.
- [x] 5.4 Run the AST-equality proof for both lifts; capture `OK` lines.
- [x] 5.5 Complexity re-measure: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' congress_videos/speaker_turn_prepare_dag.py`; confirm `_prepare_turns_callable` drops 19→≤11 (predicted 11 — still masked, no token drop this PR). State explicitly in the PR body that the counter stays at 8 (unchanged) because this lift alone does not clear the file.
- [x] 5.6 No `pyproject.toml`/`EXPECTED_C901_FILE_COUNT` edit in this PR.
- [x] 5.7 `uvx ruff check .` + `uvx ruff format --check .` + focused `uv run pytest -o addopts= tests/congress_videos/test_speaker_turn_prepare_dag.py`; DagBag import check (see Suggested Work Units) at the PR5 tip.
- [x] 5.8 Commit: `refactor(speaker-turn-prepare): lift QA-winner resolution and persistence out of _prepare_turns_callable (#272)`. PR5 body: 📍 PR5, base = PR4 branch, `Refs #272`, explicit "counter unchanged at 8, function still masked" note, and the revert-pairing note: **PR5 may only be reverted together with PR6 (or PR6 first)** — reverting it alone leaves the function at 19 in a file whose token PR6 will already have dropped.

## PR6 — `_prepare_turns_callable` VAD/sidecar/decode helper + token drop (`congress_videos/speaker_turn_prepare_dag.py`, part 2 of 2)

- [x] 6.1 Rebase onto PR5's tip; hidden-regression pre-check: `uvx ruff check --select C901 --config 'lint.per-file-ignores = {}' --output-format concise congress_videos/speaker_turn_prepare_dag.py` against `origin/dev`.
- [x] 6.2 RED-first test in `tests/congress_videos/test_speaker_turn_prepare_dag.py` for `_prepare_turn_artifacts`: `rc != 0` returns early without calling `mark_turn_prepared`; any exception raised inside is swallowed (returns `None`, never raises); `mark_turn_prepared` is the last call on the success path. Confirm RED before the lift.
- [x] 6.3 Lift `_prepare_turn_artifacts(db, turn, turn_id, output_path) -> None` from base 438-470 (whole `try/except`), immediately above `_prepare_turns_callable`. Replace the whole `try/except` with one call `_prepare_turn_artifacts(db, turn, turn_id, output_path)`, applying normalization (a) twice (loop-exit `Continue()` → `Return(None)` at base lines 457 and 470). The subprocess construction, argument handling, and timeout inside `trim_turn_silence_with_vad`/`_run_ffmpeg_decode_check` stay byte-identical and untouched by this slice.
- [x] 6.4 Run the AST-equality proof for the lift, including both (a) rewrites; capture `OK` lines.
- [x] 6.5 Complexity re-measure: `uvx ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=1' congress_videos/speaker_turn_prepare_dag.py`; confirm `_prepare_turns_callable` ≤10 (predicted 9).
- [x] 6.6 Re-run the hidden-regression check on `speaker_turn_prepare_dag.py` immediately before the token drop (multi-offender entry rule — `UP022` must remain). In the same commit as the lift, edit the `pyproject.toml` entry `["C901","UP022"]` → `["UP022"]` and decrement `EXPECTED_C901_FILE_COUNT` 8→7 in `tests/test_ruff_config.py`. Confirm both edited code lists stay sorted/deduped per `test_every_code_list_is_sorted_and_deduped`.
- [x] 6.7 `uvx ruff check .` + `uvx ruff format --check .` + focused `uv run pytest -o addopts= tests/congress_videos/test_speaker_turn_prepare_dag.py`; DagBag import check at the PR6 tip.
- [x] 6.8 Commit: `refactor(speaker-turn-prepare): lift turn-artifact preparation and drop C901 token (#272)`. PR6 body: 📍 PR6, base = PR5 branch, `Refs #272`, repeat the PR5/PR6 revert-pairing note.

## Final tip — full verification, e2e, and the slice-4 report

- [x] 7.1 With all six PRs merged to `dev` at the stack tip: run full `uv run pytest` (default addopts, coverage gate). Confirm green with coverage ≥80% against the ~4270-test baseline plus this slice's new tests.
- [x] 7.2 Confirm `pyproject.toml` carries exactly 7 `"C901"` entries and `EXPECTED_C901_FILE_COUNT == 7` in `tests/test_ruff_config.py`, and `uvx ruff check .` is clean at the tip.
- [x] 7.3 `git diff origin/dev..HEAD -- tests/` (pre-slice base) shows additions only — zero pre-existing assertions edited.
- [x] 7.4 (reported `unavailable`: Docker daemon unreachable in the run environment; NAS import check owed after `git_sync_dag`, see 7.7) Run `bash scripts/test-airflow-e2e.sh` at the final tip (PR5/PR6 touch a DagBag-parsed module). If it reports `unavailable`, note that a NAS `airflow dags list-import-errors` check after `git_sync_dag` is owed on **both** dev and prod schedulers before merge to `main`.
- [ ] 7.5 Open the release PR `dev -> main`; no `Closes #272` in any PR body across the stack (`Refs #272` only) — issue #272 stays open with 7 entries remaining.
- [ ] 7.6 Post the slice-4 report comment on issue #272 listing: shipped functions with complexity before→after (`is_procedural_turn` 12→6, `extract_announcement` 12→4, `normalize_chapter_speakers` 14→6, `_resolve_speaker_inner` 14→6, `derive_candidate_intervals` 12→7, `_prepare_turns_callable` 19→9 across PR5+PR6) and the free prune of `reap_clip_preparer_dag.py`; the counter ladder 13→12→11→10→9→8→7; and every deferral with its reason: `create_app` (both benchmark servers, 14) and `_generate_metadata` (16) — decorator/closure restructuring, a design decision not a mechanical lift; `_default_model_loader` (14) — liftable, but its entry cannot drop until `create_app` lands; `trim_turn_silence_with_vad` (12) — whole-body try/finally; `youtube_channel.py`, `download.py`, `utils/youtube_downloader.py` (multi-violator files) — sized for slice 5.
- [ ] 7.7 After merge to `dev`, trigger `git_sync_dag` on each NAS scheduler (dev and prod are separate stacks) and confirm `airflow dags list-import-errors` is empty on both.
